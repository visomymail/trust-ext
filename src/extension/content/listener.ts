import { MessageType } from "../../utils/types/messenger.types";
import { ContentStateType } from "./types/state.types";

export function listener(message: MessageType, state: ContentStateType): void {
    switch(message.channel) {

    };
};